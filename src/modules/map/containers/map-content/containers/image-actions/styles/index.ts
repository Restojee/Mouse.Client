import styled from "styled-components";

export const StyledActionsContainer = styled.div({
  display: "flex",
  gap: 10,
  right: 10,
  top: 10,
  position: "absolute",
  zIndex: 1,
  button: {
    color: "inherit",
  },
});
