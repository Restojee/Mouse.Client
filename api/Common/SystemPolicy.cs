namespace Mouse.NET.Common;

// Deprecated: all system policies moved to OtherPolicy enum.
// Kept as empty enum to avoid breaking RolePolicyType.SystemPolicy DB values during migration.
public enum SystemPolicy
{
}
